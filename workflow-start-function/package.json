{
  "scripts": {
    "package": "rm -f workflow-start-function.zip && zip -j workflow-start-function.zip src/*"
  },
  "peerDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  }
}
