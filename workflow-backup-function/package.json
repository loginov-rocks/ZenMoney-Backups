{
  "scripts": {
    "package": "rm -f workflow-backup-function.zip && zip -j workflow-backup-function.zip src/*"
  },
  "peerDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-ssm": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  }
}
